# Netlify docs: 
#   The redirects engine will process the first matching rule it 
#   finds, reading from top to bottom. Rules in the _redirects file 
#   are always processed first, followed by rules in the Netlify 
#   configuration file.

# Blog paths:
#/blog                  /blog                 200
#/blog                  /blog/404.html        404

# Blog paths:
# /static/*              /gatsby/static/:splat     200!
# /blog                  /gatsby/blog/index.html   200!
# /blog/*                /gatsby/blog/:splat       200!
# /ai-tools              /gatsby                   200!
# /ai-tools/*            /gatsby/ai-tools/:splat   200!
/blog        /blog/404.html          404
